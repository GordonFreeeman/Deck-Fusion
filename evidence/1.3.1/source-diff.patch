--- 1.3.0/main.py
+++ 1.3.1/main.py
@@ -52,7 +52,7 @@
 class Plugin:
     async def _main(self):
         self._initialize()
-        LOG.info('Deck Fusion 1.3.0 initialized, no root privilege requested.')
+        LOG.info('Deck Fusion 1.3.1 initialized, no root privilege requested.')
 
     def _initialize(self):
         if getattr(self, '_engine', None) is not None: return
@@ -93,6 +93,7 @@
                 if action == 'schema': return e.schema(appid)
                 if action == 'wine_context': return e.wine_context(payload['profile'], payload.get('launch', ''))
                 if action == 'diagnostics': return e.diagnostics(appid)
+                if action == 'game_diagnostics': return e.game_diagnostics(appid, payload.get('exe'))
                 if action == 'mod_diagnostics': return e.mod_diagnostics(appid, payload.get('exe'))
                 if action == 'sync_from_disk': return e.sync_from_disk(appid)
                 if action == 'install': return e.packages.install(payload['component'],progress,latest=bool(payload.get('latest')))
--- 1.3.0/package.json
+++ 1.3.1/package.json
@@ -1,7 +1,7 @@
 {
   "name": "deck-fusion",
-  "version": "1.3.0",
-  "description": "Optional Cyberpunk Proton prefix runtimes with snapshots, restore, logs and cancellation",
+  "version": "1.3.1",
+  "description": "Per-game graphics, Wine DLL overrides, optional Windows runtimes, diagnostics and recovery",
   "type": "module",
   "license": "MIT",
   "scripts": {
--- 1.3.0/plugin.json
+++ 1.3.1/plugin.json
@@ -8,6 +8,6 @@
       "graphics",
       "utility"
     ],
-    "description": "Per-game LSFG, OptiScaler and ReShade with custom Wine DLL overrides, one-time repair, tracked backups and footer-safe navigation."
+    "description": "Per-game LSFG, OptiScaler, ReShade, custom DLL overrides, optional Proton runtimes, read-only diagnostics and tracked recovery."
   }
 }
--- 1.3.0/src/index.js
+++ 1.3.1/src/index.js
@@ -275,7 +275,7 @@
       if(node.scrollTop!==before){e.preventDefault();e.stopPropagation();}
     },style:{...ui.box,overflowY:'auto',maxHeight:'min(230px, max(64px, calc(100vh - 356px)))'}},content);
   return h(U.ConfirmModal,{
-    strTitle:helper?'Install Protontricks helper?':restoring?'Restore the saved Proton prefix?':'Install Cyberpunk runtimes?',
+    strTitle:helper?'Install Protontricks helper?':restoring?'Restore the saved Proton prefix?':'Install selected Windows runtimes?',
     strDescription:helper?'Official Flathub package for this user':`${plan.target.game} · App ID ${plan.target.appid}`,
     strOKButtonText:helper?'Install helper':restoring?'Keep current & restore':'Back up & install',
     strCancelButtonText:'Cancel',bDisableBackgroundDismiss:true,onOK:()=>decide(true),onCancel:()=>decide(false),closeModal:()=>decide(false)},
@@ -336,7 +336,7 @@
   const [dllBatch,setDllBatch]=useState([]),[dllSearch,setDllSearch]=useState(''),[showAllDLLs,setShowAllDLLs]=useState(false);
   const [modInfo,setModInfo]=useState(null),[showModLogs,setShowModLogs]=useState(false);
   const [runtimeInfo,setRuntimeInfo]=useState(null),[runtimePrefix,setRuntimePrefix]=useState('');
-  const [runtimeVerbs,setRuntimeVerbs]=useState(['d3dcompiler_47','vcrun2022']);
+  const [runtimeVerbs,setRuntimeVerbs]=useState([]);
   const [runtimeJob,setRuntimeJob]=useState(null),[showRuntimeLog,setShowRuntimeLog]=useState(false);
   const [showRuntimeFiles,setShowRuntimeFiles]=useState(false),[showHelperLog,setShowHelperLog]=useState(false);
   const modalRef=useRef(null);
@@ -347,7 +347,7 @@
   useEffect(()=>{
     let active=true;setWineError('');
     const owner=`${p?.appid||''}|${p?.exe||''}`;
-    if(wineOwner.current!==owner){wineOwner.current=owner;setWineInfo(null);setDllBatch([]);setDllSearch('');setDllName('');setShowAllDLLs(false);setModInfo(null);setShowModLogs(false);setRuntimeInfo(null);setRuntimePrefix('');setShowRuntimeLog(false);setShowRuntimeFiles(false);setShowHelperLog(false);}
+    if(wineOwner.current!==owner){wineOwner.current=owner;setWineInfo(null);setDllBatch([]);setDllSearch('');setDllName('');setShowAllDLLs(false);setModInfo(null);setShowModLogs(false);setRuntimeInfo(null);setRuntimePrefix('');setRuntimeVerbs([]);setShowRuntimeLog(false);setShowRuntimeFiles(false);setShowHelperLog(false);}
     if(!p?.exe||observed===null){setWineLoading(false);return;}
     // Keep same-game controls mounted while recalculating proposals. Removing
     // the entire inventory here caused focus loss and vertical layout jumps.
@@ -448,14 +448,14 @@
     });
   }
   async function runtimePayload(prefix=runtimePrefix){
-    if(!p?.exe||!/cyberpunk2077\.exe$/i.test(p.exe))throw new Error('Select Cyberpunk2077.exe first.');
+    if(!p?.exe)throw new Error('Select a game and its Windows executable in Library first.');
     return {appid:p.appid,exe:p.exe,prefix:prefix||null,launch:await readLaunch(apps(),p.appid),runtimes:[...runtimeVerbs]};
   }
   async function inspectRuntime(prefix=runtimePrefix){
     const payload=await runtimePayload(prefix);
     const info=await job('runtime_status',payload);
     if(mounted.current){setRuntimeInfo(info);setRuntimePrefix(info.prefix||'');}
-    status('Prefix inspection complete. File presence does not establish successful mod loading.');
+    status('Prefix inspection complete. File presence does not establish successful game or mod loading.');
     return info;
   }
   async function confirmRuntime(plan){
@@ -619,6 +619,7 @@
     h('div',{style:ui.muted},`${matchingGames.length} matching game${matchingGames.length===1?'':'s'}${matchingGames.length>250?'. First 250 shown; narrow the search to find the rest.':'.'}`),
     button('Open setup wizard',()=>run(resumeWizard),busy,'Guided game selection, features, frame targets, components and one-click application.'),
     p&&note(`${p.name}\n${p.root}`),
+    p&&button('Open runtime setup',()=>setTab('runtimes'),busy,'Optional d3dcompiler_47 / Visual C++ runtime installation for this game’s Proton prefix. No graphics features need to be enabled.'),
     p&&select('Actual executable',p.exe,(scan?.candidates||[]).map(c=>({label:`${c.relative} (${c.bits}-bit, ${c.api})${c.support?' · support tool':c.launcher?' · launcher':''}`,data:c.path})),path=>change(d=>{d.exe=path;}),'Choose the rendering executable, not a launcher. The scan includes nested bin/x64 and Binaries/Win64 folders.','No executable was detected. Enter its exact path below and check the scan warnings.',busy),
     p&&scan?.candidates?.[0]&&button(`Use recommended executable: ${scan.candidates[0].relative}`,()=>change(d=>{d.exe=scan.candidates[0].path;}),busy),
     ...(scan?.warnings||[]).map((warning,i)=>h('div',{key:`scan-warning-${i}`},note(warning,true))),
@@ -668,7 +669,7 @@
       'Selects ManualInputPolling=true and DisableOverlays=false. Does not remap your Steam controller layout.'),
     select('OptiScaler mouse input',p.opti.mouse_input||'auto',[
       {label:'Keep upstream / advanced setting',data:'auto'},{label:'Manual polling (mouse workaround)',data:'polling'},{label:'Window messages (standard)',data:'window'}],v=>change(d=>{d.opti.mouse_input=v;}),
-      'Polling bypasses missed window messages, but cannot block game input. Pause the game and close CET/ReShade menus before editing OptiScaler.'),
+      'Polling bypasses missed window messages, but cannot block game input. Pause the game and close other mod/ReShade menus before editing OptiScaler.'),
     select('Steam Input and other overlays',p.opti.steam_input||'auto',[
       {label:'Keep upstream / advanced setting',data:'auto'},{label:'Keep Steam Input / overlays enabled',data:'keep'},{label:'Let OptiScaler disable other overlays',data:'disable'}],v=>change(d=>{d.opti.steam_input=v;}),
       'Keeping Steam Input helps mouse-emulation mappings. Some frame-generation paths are less compatible with other overlays. Apply and restart.'),
@@ -723,6 +724,7 @@
   const visibleDLLs=localDLLs.filter(file=>file.name.toLowerCase().includes(dllSearch.toLowerCase())&&
     (showAllDLLs||dllSearch.trim()||commonProxyNames.has(file.module)||dllBatch.includes(file.module))).slice(0,64);
   const cyberpunk=/cyberpunk2077\.exe$/i.test(p?.exe||'');
+  const nativeGame=(scan?.candidates||[]).find(candidate=>candidate.path===p?.exe)?.kind==='elf';
   const userDLLs=customEntries(p?.wine?.custom_overrides);
   const winePage=p?section('Custom Wine DLL overrides',
     note('Keep your existing mod loaders alongside OptiScaler and ReShade. Existing launch overrides and Wine-prefix settings are inspected, not silently removed.'),
@@ -756,9 +758,10 @@
     dllBatch.length>0&&button('Clear selection',()=>setDllBatch([]),busy,'Clears only the checkboxes. Existing overrides and files are untouched.'),
     text('DLL module names',dllName,setDllName,'One or more names, for example dxgi.dll, version.dll, winmm.dll. Comma-separated; no paths.'),
     button('Add / update custom overrides',addOverride,busy||!dllName.trim(),'Adds all entered names. An existing entry for the same exact name takes the selected load order; all other entries stay unchanged.'),
-    cyberpunk&&button('Open Cyberpunk runtime setup',()=>{setTab('runtimes');run(()=>inspectRuntime());},busy,'Check or install d3dcompiler_47 and vcrun2022 inside the Proton prefix, with a snapshot first.'),
-    cyberpunk&&button('Check Cyberpunk mod files and logs',()=>run(async()=>{setModInfo(await rpc('mod_diagnostics',{appid:p.appid,exe:p.exe}));status('Mod file inventory loaded. Presence is not proof that a mod ran successfully.');}),busy,'Read-only checks for CET, RED4ext, redscript and their own logs. No dependency downloads or script-cache deletion.'),
-    modInfo&&h('div',{'data-mod-diagnostics':true,style:ui.box},h('h3',null,'Cyberpunk mod evidence'),h('p',null,modInfo.note),
+    button('Open runtime setup',()=>{setTab('runtimes');},busy,'Check or install d3dcompiler_47 and vcrun2022 inside the Proton prefix, with a snapshot first.'),
+    button('Check game files and logs',()=>run(async()=>{setModInfo(await rpc('game_diagnostics',{appid:p.appid,exe:p.exe}));status('Game file inventory loaded. No files or settings were changed.');}),busy||!p.exe,'Read-only checks for this executable, nearby graphics configuration, DLLs and OptiScaler/ReShade logs. Available for every game.'),
+    cyberpunk&&section('Optional game-specific checks',button('Check Cyberpunk mod files and logs',()=>run(async()=>{setModInfo(await rpc('mod_diagnostics',{appid:p.appid,exe:p.exe}));status('Mod file inventory loaded. Presence is not proof that a mod ran successfully.');}),busy,'Extra checks for this game only: CET, RED4ext, redscript and their own logs. No dependency downloads or script-cache deletion.')),
+    modInfo&&h('div',{'data-mod-diagnostics':true,style:ui.box},h('h3',null,modInfo.title||'Game files and logs'),h('p',null,modInfo.note),
       ...modInfo.frameworks.map(item=>modCard(item.name,h('strong',null,item.name),
         ...item.files.map(file=>h('div',{key:file.path,style:ui.mono},`${file.present?'Found':'Not found'}: ${file.path}${file.managed?' (Deck Fusion tracked)':''}`,file.note&&h('div',{style:ui.muted},file.note))),
         h('div',{style:ui.muted},item.note),h('div',{style:ui.muted},`${item.children.length} folder/file entries: ${item.children.slice(0,24).join(', ')||'none'}${item.children.length>24?'…':''}`))),
@@ -766,7 +769,7 @@
         h('div',{style:ui.mono},log.path),log.modified&&h('div',{style:ui.muted},`Modified ${new Date(log.modified*1000).toLocaleString()} · ${log.current_launch===true?'updated since last recorded launch':log.current_launch===false?'older than last recorded launch':'launch freshness unknown'}`),
         log.error_lines?.length>0&&h('div',null,h('div',{style:ui.muted},'Error-like lines in this bounded log tail (may be historical):'),code(log.error_lines.join('\n'))),
         log.note&&note(log.note,true),showModLogs&&log.present&&code(log.tail))),
-      toggle('Show recent mod log text',showModLogs,setShowModLogs,'Up to 12 KB / 60 lines per log. These logs can contain older messages.'),
+      toggle('Show recent log text',showModLogs,setShowModLogs,'Up to 12 KB / 60 lines per log. These logs can contain older messages.'),
       ...(modInfo.notes||[]).map((n,i)=>h('div',{key:i},note(n,true)))),
     wineInfo?.proposed&&h('div',{style:ui.box},h('strong',null,wineLoading?'Proposed graphics loaders (refreshing…)':wineError?'Previous graphics loader proposal (refresh failed)':'Proposed graphics loaders'),
       h('div',null,p.opti.enabled?`OptiScaler: ${wineInfo.proposed.opti_proxy}.dll`:'OptiScaler: off'),
@@ -793,9 +796,15 @@
     diagnostic&&code(diagnostic),
     button('Recover unfinished operation',()=>run(recover),busy),
     note('Restore managed games before uninstalling. The persistent wrapper and backups survive plugin removal so launch entries are not stranded.')):note('Select a game in Library.');
-  const runtimePage=p&&cyberpunk?section('Cyberpunk Proton runtimes',
-    note('d3dcompiler_47 and the Visual C++ 2022 runtime belong in the game’s Proton prefix, not bin/x64. Their absence from bin does not mean they are missing. This page is separate from graphics Apply.'),
-    button('Check prefix and runtimes',()=>run(()=>inspectRuntime()),busy,'Reads the current Steam launch options, detected prefixes, helper availability, files and installation receipts. No installers are run.'),
+  const runtimePage=!p?note('Select a game in Library to inspect its Windows runtime requirements. Nothing is installed automatically.'):
+    (!p.exe||nativeGame)?section('Windows runtimes',
+      note(`${p.name} · App ID ${p.appid}`),
+      note(nativeGame?'This is a native Linux executable. Windows runtimes are not applicable. To maintain a Windows version, select its executable and use an initialized Proton prefix.':'Select this game’s Windows executable in Library first. Runtime installation requires an initialized Proton prefix.'),
+      button('Choose a Windows executable',()=>setTab('library'),busy,'Return to Library. No game files, prefix or compatibility settings have been changed.')):
+    section('Windows runtimes',
+    note(`${p.name} · App ID ${p.appid}`),
+    note('Optional compatibility tools for any Windows game using Proton, including Steam-managed non-Steam shortcuts. Install only dependencies required by the game or its mods. These libraries belong in its prefix, not its game folder. This page is separate from graphics Apply.'),
+    button('Check prefix and runtimes',()=>run(()=>inspectRuntime()),busy||!p.exe||nativeGame,'Reads the current Steam launch options, detected prefixes, helper availability, files and installation receipts. No installers are run.'),
     !runtimeInfo&&note('Start with Check prefix and runtimes. Choose an existing initialized prefix; this workflow never creates or resets one.'),
     runtimeInfo&&h(R.Fragment,null,
       select('Proton prefix',runtimePrefix,runtimeInfo.prefixes.map(path=>({label:path,data:path})),path=>{setRuntimePrefix(path);run(()=>inspectRuntime(path));},
@@ -815,7 +824,7 @@
     ...['d3dcompiler_47','vcrun2022'].map(verb=>toggle(verb,runtimeVerbs.includes(verb),enabled=>{
       if(!busyRef.current)setRuntimeVerbs(old=>enabled?[...new Set([...old,verb])]:old.filter(v=>v!==verb));
     },verb==='d3dcompiler_47'?'Microsoft D3D shader compiler. Use the upstream installer recipe, not a random DLL download.':'Microsoft Visual C++ runtime. May conflict with an existing VC installation; installation is optional, backed up and not automatically forced.',busy)),
-    note('A full prefix snapshot is made first. Links to external save folders are preserved, not copied. Close Cyberpunk and other Protontricks windows, and do not launch the game or change Proton while this runs. Runtime downloads need internet access.',true),
+    note('A full prefix snapshot is made first. Links to external save folders are preserved, not copied. Close the selected game and other Protontricks windows, and do not launch the game or change Proton while this runs. Runtime downloads need internet access.',true),
     button('Install selected runtimes',()=>run(()=>requestRuntime(false)),busy||!runtimeVerbs.length||!runtimeInfo?.helper?.available||!runtimePrefix||!!runtimeInfo?.blockers?.length,
       'Review the exact target, dependencies and backup size, then choose Back up & install. Existing mod-loader DLLs and launch overrides remain untouched.'),
     runtimeInfo?.helper_log&&h('div',{style:ui.box},
@@ -830,12 +839,12 @@
       h('div',{style:ui.muted},'Installation receipts are evidence, not a game-launch guarantee.')),
     runtimeInfo?.restore_snapshot?.snapshot_ready&&runtimeInfo.restore_snapshot.state!=='restored'&&button('Restore latest runtime snapshot',()=>run(()=>requestRuntime(true)),busy||!!runtimeInfo.blockers?.length,
       'Separate confirmation. Restores the latest completed snapshot for the selected prefix and retains today’s pfx alongside it. Prefix-local saves/settings revert; external linked folders are untouched.'),
-    note('Successful runtime installation is not proof that CET, RED4ext or scripts load. Re-extracting mods can overwrite a graphics proxy. Check DLLs and mod logs rather than force-overwriting a loader. Winetricks --force is not used because it can also bypass checksum errors.'),
-    button('Open DLLs and mod diagnostics',()=>setTab('wine'),busy)
-  ):note('Select Cyberpunk2077.exe in Library to use the Cyberpunk runtime workflow. Other games and prefixes are not modified.');
+    note('These are optional dependencies, not a universal launch fix. Existing runtimes or the game’s own installer may already provide them. Check game and mod logs before changing a working prefix. Winetricks --force is not used because it can also bypass checksum errors.'),
+    button('Open DLLs and game diagnostics',()=>setTab('wine'),busy)
+  );
 
   const tools=section('Components, updates and setup',
-    note('Deck Fusion 1.3.0. HTTPS uses the SteamOS trust store plus a bundled Mozilla CA fallback, with certificate and hostname verification enabled. No root flag or SteamOS read-only changes.'),
+    note('Deck Fusion 1.3.1. HTTPS uses the SteamOS trust store plus a bundled Mozilla CA fallback, with certificate and hostname verification enabled. No root flag or SteamOS read-only changes.'),
     note(state.bundled?.complete?'Offline archives are included. Absent components are prepared locally on first open. Existing installed versions are kept.':(state.bundled?.note||`Offline third-party archives are not embedded. Missing components: ${(state.bundled?.missing||[]).join(', ')||'none'}. Tools downloads verified upstream copies on demand; the certificate fallback is included.`),!state.bundled?.complete),
     ...(state.bundled?.errors||[]).map((message,i)=>h('div',{key:`bundle-error-${i}`},note(message,true))),
     ...[['lsfg','LSFG'],['opti','OptiScaler'],['reshade','ReShade']].map(([key,label])=>row(h('div',{style:ui.box},h('strong',null,label),h('div',{style:ui.muted},state.packages[key]?`${state.packages[key].version} · ${state.packages[key].source||'existing cache'}`:'Not installed'),updates?.[key]&&h('div',{style:ui.muted},updates[key].error||`Latest available: ${updates[key].version}${updates[key].different?' · update available':' · no version change'}`)),`component-${key}`)),
@@ -893,6 +902,7 @@
     p&&toggle('Smoother motion with LSFG',p.lsfg.enabled,v=>change(d=>{d.lsfg.enabled=v;}),'Requires your own Lossless Scaling copy on Steam’s lsfg-vk branch.'),
     p&&toggle('Replace the game’s upscaler with OptiScaler',p.opti.enabled,v=>change(d=>{d.opti.enabled=v;}),'For compatible 64-bit games with DLSS, FSR or XeSS inputs. Not a universal graphics enhancer.'),
     p&&toggle('Add ReShade effects',p.reshade.mode!=='off',v=>change(d=>{d.reshade.mode=v?(d.opti.enabled?'opti':'standalone'):'off';}),'Use sharpening, contrast or colour effects. Effects can reduce base FPS.'),
+    button('Optional Windows runtime setup',()=>{setWizard(false);setTab('runtimes');},busy,'Check d3dcompiler_47 / Visual C++ dependencies for this game. Nothing is installed automatically. Resume wizard retains this draft.'),
     button('Configure mod-loader DLL overrides',()=>{setWizard(false);setTab('wine');},busy,'Reads existing overrides and local DLLs. Resume wizard keeps this draft.'),
     note('Conflicting settings are shown and resolved together in the final Apply dialog, not through a series of confirmation toggles.')):
   wizardStep===2?section('Set your targets',
@@ -932,11 +942,11 @@
       !wizardDone&&h('div',{style:{flex:1}},compact('Back',()=>setWizardStep(step=>Math.max(0,step-1)),busy||wizardStep===0)),
       !wizardDone&&h('div',{style:{flex:1}},compact(wizardStep===4?'Apply this game':'Next',()=>run(async()=>{if(wizardStep===4){if(await requestApply(false))setWizardDone(true);}else await nextWizard();}),busy||!p||(wizardStep===0&&!p.exe)||(wizardStep===3&&!readyComponents)))));
 
-  const tabs=[['library','Library',library],['lsfg','LSFG',lsfg],['opti','OptiScaler',opti],['reshade','ReShade',reshade],['wine','DLLs',winePage],['runtimes','Runtimes',runtimePage],['apply','Apply',applyPage],['tools','Tools',tools]].map(([id,title,content])=>({id,title,content:h('div',{'data-df-scroll':id,onFocusCapture:scrollFocusedControl,style:{height:'100%',minHeight:0,overflowY:'auto',overflowX:'hidden',overscrollBehavior:'contain',scrollPaddingBlock:14,padding:'0 24px 24px',boxSizing:'border-box'}},content)}));
+  const tabs=[['library','Library',library],['lsfg','LSFG',lsfg],['opti','OptiScaler',opti],['reshade','ReShade',reshade],['wine','DLLs',winePage],['runtimes','Runtimes',runtimePage],['apply','Apply',applyPage],['tools','Tools',tools]].map(([id,title,content])=>({id,title,content:h('div',{key:`${id}:${p?.appid||''}:${p?.exe||''}`,'data-df-scroll':id,onFocusCapture:scrollFocusedControl,style:{height:'100%',minHeight:0,overflowY:'auto',overflowX:'hidden',overscrollBehavior:'contain',scrollPaddingBlock:14,padding:'0 24px 24px',boxSizing:'border-box'}},content)}));
   if(!wizard&&!U.Tabs)return h('div',{style:{padding:40}},note('Decky native Tabs is unavailable. Update Decky Loader before using this build.',true));
   return h('div',{ref:frameRef,'data-df-frame':true,style:{paddingTop:42,height:frameHeight,boxSizing:'border-box',color:'#e8eef5',display:'flex',flexDirection:'column',minWidth:0,overflow:'hidden'}},
     h('div',{style:{padding:'0 28px 8px',display:'flex',alignItems:'center',justifyContent:'space-between',gap:16,flexShrink:0}},
-      h('div',{style:{flex:1,minWidth:0}},h('strong',{style:{fontSize:22}},'Deck Fusion'),h('div',{title:p?.name,style:{...ui.muted,overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}},p?`${p.name}${JSON.stringify(p)!==baseline?' · draft changes':''}`:'Per-game graphics control')),
+      h('div',{style:{flex:1,minWidth:0}},h('strong',{style:{fontSize:22}},'Deck Fusion'),h('div',{title:p?.name,style:{...ui.muted,overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}},p?`${p.name}${JSON.stringify(p)!==baseline?' · draft changes':''}`:'Per-game graphics and compatibility tools')),
       h(U.Focusable,{style:{display:'flex',gap:8,flexShrink:0}},
         !wizard&&h('div',{style:{width:140}},compact(wizardStep>0?'Resume wizard':'Setup wizard',()=>run(resumeWizard),busy)),
         !wizard&&h('div',{style:{width:135}},compact('Apply settings',()=>run(()=>requestApply(false)),busy||!p)),
@@ -946,7 +956,7 @@
     runtimeJob&&h('div',{style:{padding:'0 28px 8px',flexShrink:0}},h(U.DialogButton||U.ButtonItem,{onClick:cancelRuntime,style:{width:'100%'}},'Cancel runtime operation')),
     h('div',{style:{flex:1,minHeight:0,overflow:'hidden'}},wizard?wizardView:h(U.Tabs,{tabs,activeTab:tab,onShowTab:next=>{if(!busyRef.current)setTab(next);},autoFocusContents:true})));
 }
-function QuickPanel(){return section('Deck Fusion',note('Per-game LSFG, upscaling and ReShade. Configuration opens in a full-screen, controller-friendly view.'),button('Set up a game',()=>{U.Navigation.Navigate(route+'/setup');U.Navigation.CloseSideMenus();}),button('Open Deck Fusion',()=>{U.Navigation.Navigate(route);U.Navigation.CloseSideMenus();}),row(h('div',{style:ui.muted},'1.3.0 · Research-backed installation paths, not a universal game compatibility guarantee.')));}
+function QuickPanel(){return section('Deck Fusion',note('Per-game graphics, DLL overrides, Windows runtimes and recovery. Configuration opens in a full-screen, controller-friendly view.'),button('Set up a game',()=>{U.Navigation.Navigate(route+'/setup');U.Navigation.CloseSideMenus();}),button('Open Deck Fusion',()=>{U.Navigation.Navigate(route);U.Navigation.CloseSideMenus();}),row(h('div',{style:ui.muted},'1.3.1 · Per-game graphics, DLLs, runtimes and recovery. Compatibility depends on the game.')));}
 export default function(){
   api.routerHook.addRoute(route,()=>h(ScreenBoundary,null,h(Manager)));
   api.routerHook.addRoute(route+'/setup',()=>h(ScreenBoundary,null,h(Manager,{startWizard:true})));
--- 1.3.0/py_modules/fusion_engine.py
+++ 1.3.1/py_modules/fusion_engine.py
@@ -15,6 +15,7 @@
 from fusion_launch import compose_launch, restore_launch, launch_environment, shell_tokens, merge_dll_overrides, parse_dll_overrides, launch_assignments
 from fusion_policy import hardware_info, resolve_settings
 from fusion_mods import cyberpunk_diagnostics
+from fusion_game_diagnostics import game_diagnostics
 from fusion_prereqs import PrefixRuntimes
 from fusion_wine import inspect_overrides, resolve_loaders, guard_payload, RESH_PROXIES
 from fusion_shaders import opti_schema, pack_files, preset_text
@@ -715,27 +716,38 @@
             p['reshade']['performance'] = ini.get('GENERAL','PerformanceMode',fallback='1') == '1'
         return p
 
-    def mod_diagnostics(self, appid, selected_exe=None):
+    def _diagnostic_target(self, appid, selected_exe=None):
         # Inspect the current draft, not only the last applied profile. Keep the
         # installation root authoritative and reject symlinks/paths outside it.
         game = self.game(appid)
         root = Path(game['root']).resolve()
         value = selected_exe if selected_exe is not None else self.profile(appid).get('exe', '')
         if not isinstance(value, str) or not value:
-            raise FusionError('Select Cyberpunk2077.exe before inspecting mod files.')
+            raise FusionError('Select a game executable in Library before inspecting its files.')
         candidate = Path(value).expanduser()
         if not candidate.is_absolute(): candidate = root / candidate
         if not candidate.is_relative_to(root):
             raise FusionError('The diagnostic executable must be inside this game installation.')
         candidate = safe_target(root, candidate.relative_to(root).as_posix())
         if not candidate.is_file(): raise FusionError('The selected game executable no longer exists.')
+        return root, candidate
+
+    def game_diagnostics(self, appid, selected_exe=None):
+        root, candidate = self._diagnostic_target(appid, selected_exe)
+        result = game_diagnostics(root, candidate, self.store(appid))
+        result.update(game=self.game(appid)['name'], selected_exe=str(candidate))
+        return result
+
+    def mod_diagnostics(self, appid, selected_exe=None):
+        # Optional adapter for this game's known frameworks, not a core-tool gate.
+        root, candidate = self._diagnostic_target(appid, selected_exe)
         result = cyberpunk_diagnostics(root, str(candidate), self.store(appid))
-        result['selected_exe'] = str(candidate)
+        result.update(title='Optional Cyberpunk mod evidence', selected_exe=str(candidate))
         return result
 
     def diagnostics(self, appid=''):
         status = self.packages.status()
-        result = {'version':'1.3.0','data':str(self.data),'home':str(self.home),
+        result = {'version':'1.3.1','data':str(self.data),'home':str(self.home),
                   'packages':status,'legacy_layers':self.old_layers(),
                   'settings':self.settings(), 'bundled':self.packages.bundle_status(),
                   'https':tls_context()[1], 'last_error':read_json(self.data/'last-error.json',None),
--- 1.3.0/py_modules/fusion_game_diagnostics.py
+++ 1.3.1/py_modules/fusion_game_diagnostics.py
@@ -0,0 +1,90 @@
+"""Game-independent, read-only evidence from a validated installation.
+
+No game names/AppIDs or assumed mod frameworks. Only fixed graphics filenames
+and a bounded non-recursive local inventory are read. No payload is executed.
+"""
+from __future__ import annotations
+from pathlib import Path
+import re
+
+from fusion_steam import pe_info
+from fusion_util import FusionError, read_json, safe_target
+
+
+def game_diagnostics(root: Path, exe: Path, store: Path) -> dict:
+    root = root.resolve()
+    parent = exe.parent.relative_to(root)
+    manifest = read_json(store / 'manifest.json', {}).get('files', {})
+    tracked = {name.casefold() for name in manifest}
+    launch = read_json(store / 'last-launch.json', {})
+    launched = launch.get('time')
+    if not isinstance(launched, (int, float)): launched = None
+    notes = []
+
+    def relative(name):
+        return (parent / name).as_posix()
+
+    def artifact(name):
+        item = {'path': name, 'present': False, 'managed': name.casefold() in tracked}
+        try:
+            path = safe_target(root, name)
+            item['present'] = path.is_file()
+        except (OSError, FusionError) as error:
+            item['note'] = str(error)
+        return item
+
+    def names(name):
+        result = []
+        try:
+            # '.' means the authoritative, already resolved game root, not input traversal.
+            folder = root if name == '.' else safe_target(root, name)
+            if folder.is_dir():
+                for index, item in enumerate(folder.iterdir()):
+                    if index >= 4096:
+                        notes.append(f'{name}: inventory limited to 4096 entries.')
+                        break
+                    if not item.is_symlink() and (item.is_file() or item.is_dir()):
+                        result.append(item.name)
+        except (OSError, FusionError) as error:
+            notes.append(str(error))
+        return sorted(result, key=str.casefold)
+
+    local_dlls = [relative(name) for name in names(parent.as_posix())
+                  if name.casefold().endswith(('.dll', '.asi'))]
+    if len(local_dlls) > 64:
+        notes.append('Local DLL/ASI evidence limited to 64 files. Use the DLLs filter for a specific loader.')
+    image = pe_info(exe)
+    reports = [
+        {'name': 'Selected executable', 'files': [artifact(exe.relative_to(root).as_posix())],
+         'children': [], 'note': f"{image['bits']}-bit {image['kind']} file. Detected rendering API: {image['api']}. Detection is a hint, not a runtime check."},
+        {'name': 'Graphics configuration', 'files': [artifact(relative(name)) for name in
+             ('OptiScaler.ini', 'ReShade.ini', 'DeckFusionPreset.ini')], 'children': [],
+         'note': 'Optional configuration beside this executable. An absent file is not a missing game dependency.'},
+        {'name': 'Local DLLs and plugins', 'files': [artifact(name) for name in local_dlls[:64]],
+         'children': names(relative('plugins'))[:64],
+         'note': 'File inventory only. A DLL filename does not identify a mod or prove that it loaded. Plugin entries are limited to 64.'},
+    ]
+    logs = []
+    for label, name in (('OptiScaler', 'OptiScaler.log'), ('ReShade', 'ReShade.log')):
+        name = relative(name)
+        result = {'name': label, 'path': name, 'present': False, 'modified': None,
+                  'current_launch': None, 'tail': '', 'error_lines': []}
+        try:
+            path = safe_target(root, name)
+            if path.is_file():
+                info = path.stat()
+                start = max(0, info.st_size - 12288)
+                with path.open('rb') as handle:
+                    handle.seek(start)
+                    lines = handle.read(12288).decode('utf-8', 'replace').splitlines()
+                if start and lines: lines = lines[1:]
+                result.update(present=True, modified=info.st_mtime, size=info.st_size,
+                              current_launch=(info.st_mtime >= launched - 2) if launched else None,
+                              tail='\n'.join(lines[-60:]),
+                              error_lines=[line for line in lines if re.search(r'\berror\b|\bfatal\b|\bfailed\b', line, re.I)][-8:])
+        except (OSError, FusionError) as error:
+            result['note'] = str(error)
+        logs.append(result)
+    return {'title': 'Game files and logs', 'frameworks': reports, 'logs': logs,
+            'last_launch_time': launched, 'notes': notes,
+            'note': 'Read-only evidence from the selected executable’s folder. Logging may be disabled or use a custom path; missing logs do not prove a failure. Historical log messages are not a current game/mod verdict. No files, runtimes or settings were changed.'}
--- 1.3.0/py_modules/fusion_prereqs.py
+++ 1.3.1/py_modules/fusion_prereqs.py
@@ -23,7 +23,7 @@
 import uuid
 
 from fusion_launch import launch_assignments
-from fusion_steam import running_game, steam_roots, libraries
+from fusion_steam import running_game, steam_roots, libraries, pe_info
 from fusion_util import FusionError, atomic_json, read_json, safe_target, sha256
 from fusion_wine import inspect_overrides
 
@@ -145,14 +145,17 @@
         game = self.e.game(appid)
         exe = payload.get('exe')
         if not isinstance(exe, str) or not exe:
-            raise FusionError('Select Cyberpunk2077.exe before prefix maintenance.')
+            raise FusionError('Select the game’s Windows executable in Library before prefix maintenance.')
         root = Path(game['root']).resolve()
         path = Path(exe)
         if not path.is_absolute(): path = root / path
         if not path.is_relative_to(root): raise FusionError('Executable is outside this game installation.')
         path = safe_target(root, path.relative_to(root).as_posix())
-        if not path.is_file() or path.name.casefold() != 'cyberpunk2077.exe':
-            raise FusionError('This runtime workflow is for the selected Cyberpunk2077.exe only.')
+        if not path.is_file():
+            raise FusionError('The selected game executable no longer exists. Refresh Library.')
+        image = pe_info(path)
+        if image['kind'] != 'pe' or image['bits'] not in (32, 64):
+            raise FusionError('Windows runtime setup needs a 32-bit or 64-bit Windows executable and its initialized Proton prefix. Native Linux games and emulator programs do not use these Windows dependencies; select the Windows build only when you intend to run it through Proton.')
         launch = payload.get('launch', '')
         if not isinstance(launch, str) or len(launch) > 32768: raise FusionError('Invalid launch text.')
         context = inspect_overrides(self.e, {'appid': appid, 'exe': str(path), 'wine': {}}, launch)
@@ -194,7 +197,7 @@
         if (self.e.store(target['appid']) / 'pending.json').exists():
             raise FusionError('Finish or recover the pending graphics-file transaction in Apply before changing runtimes.')
         if running_game(Path(target['exe']), target['appid'])['running']:
-            raise FusionError('Close Cyberpunk before changing its prefix.')
+            raise FusionError(f"Close {target['game']} before changing its prefix.")
         processes = prefix_processes(Path(target['prefix']), target['appid'])
         if processes:
             raise FusionError('The selected prefix still has active Proton/Wine processes: ' +
@@ -214,7 +217,7 @@
                     f = prefix / 'drive_c/windows' / arch / dll
                     files.append({'path': f'drive_c/windows/{arch}/{dll}', 'present': f.is_file(), 'symlink': f.is_symlink()})
             result.append({'name': name, 'recorded': name in receipt, 'files': files,
-                           'note': 'A Winetricks receipt or file presence is not proof of native provenance, correct version or successful mod initialization.'})
+                           'note': 'A Winetricks receipt or file presence is not proof of native provenance, correct version or successful game/mod initialization.'})
         return result
 
     def validate_record(self, record, target, same_prefix=False):
@@ -252,7 +255,7 @@
         return {**{k: target[k] for k in ('appid', 'game', 'exe', 'prefixes', 'prefix', 'notes')},
                 'helper': self.helper(), 'runtimes': self.evidence(target['prefix']), 'blockers': blockers,
                 'last': receipt, 'restore_snapshot': snapshot, 'helper_log': helper_log,
-                'note': 'These libraries belong inside the Proton prefix, not bin/x64. This operation does not repair or replace your mod-loader DLLs.'}
+                'note': 'These optional Windows libraries belong inside the selected game’s Proton prefix, not its game folder. This operation does not repair or replace mod-loader DLLs.'}
 
     def options(self, payload):
         verbs = payload.get('runtimes', list(VERBS))
--- 1.3.0/py_modules/fusion_runtime.py
+++ 1.3.1/py_modules/fusion_runtime.py
@@ -38,7 +38,7 @@
         log = root / 'profiles' / argv[1] / 'last-launch.json'
         summary = {'command_executable': command[0], 'settings': {k: env.get(k) for k in (
             'WINEDLLOVERRIDES', 'DECK_FUSION_LSFG', 'LSFGVK_CONFIG', 'DXVK_FRAME_RATE', 'VKD3D_FRAME_RATE', 'PROTON_ENABLE_NVAPI', 'ENABLE_GAMESCOPE_WSI', 'DISABLE_GAMESCOPE_WSI', 'GAMESCOPE_WSI_FRAME_LIMITER_AWARE', 'GAMESCOPE_WAYLAND_DISPLAY', 'GAMESCOPE_LIMITER_FILE', 'MESA_VK_WSI_PRESENT_MODE', 'PROTON_FSR4_INDICATOR', 'FSR4_WATERMARK', 'FSR_WATERMARK', 'MLSR-WATERMARK')}}
-        summary['wrapper_version'] = '1.3.0'
+        summary['wrapper_version'] = '1.3.1'
         summary['time'] = time.time()
         summary['watermark_strategy'] = 'SDK variable absent when off; paired OptiScaler INI uses auto'
         summary['limiter_route_requested'] = ('mesa-fifo-without-gamescope-wsi'
